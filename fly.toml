# fly.toml app configuration file generated for clover-carrossel on 2026-04-21T16:51:33-03:00
#
# See https://fly.io/docs/reference/configuration/ for information about how to use this file.
#

app = 'clover-carrossel'
primary_region = 'gru'

[build]
  builder = 'paketobuildpacks/builder:base'

[processes]
  worker = 'python bot_carrossel.py'

[[vm]]
  memory = '256mb'
  cpu_kind = 'shared'
  cpus = 1
  memory_mb = 256
